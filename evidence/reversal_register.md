## Reversal Register
| overturned_decision | why_it_was_overturned | review_miss_cause | reversal_risk | remediation_owner | re_close_proof |
|---|---|---|---|---|---|
| `ADR-006 deep conformance closure treated as done despite SUCCESS + NEEDS_TEST for R42 lock-cleanup` | The earlier record declared `status: SUCCESS` / "Implementation demonstrates strong conformance" while still carrying an unproven blocker-class behavioral invariant: lock cleanup when config-reload removes a server during in-flight recovery. Under the upgraded control-plane rule, blocker-class runtime proof cannot ride inside a success-state as deferred evidence. | Review accepted inventory completeness and source inspection as closure even though the gate already knew `R42` still needed integration proof. This was a review-process miss, not just missing tests. | The same pattern can recur for any race-condition or cleanup invariant if a review artifact allows `SUCCESS` plus embedded `NEEDS_TEST` language. | `debt_closure.runtime_evidence` | Live runtime evidence showing config-reload-remove during recovery yields `config_missing=true` and leaves no stale per-server recovery lock; re-close must explicitly show blocker removed, not merely noted. |
| `ADR-006 deep conformance closure treated as done despite SUCCESS + NEEDS_TEST for R13 no-lock-during-await` | The earlier closure assumed code-reading was enough to retire the claim that `_registry_lock` is not held during awaited network I/O. The upgraded rule overturns that assumption because lock-safety is behavioral proof debt, not documentation debt. | The review shape let "analysis confirms" stand in for runtime witness evidence on a concurrency invariant. The gate did not force a contention probe before allowing done-state. | Other "MUST NOT hold lock during await/I/O" claims elsewhere could be falsely closed by static reasoning alone if this reversal is not recorded. | `debt_closure.runtime_evidence` | Runtime tracing or contention test demonstrating awaited network operations proceed without `_registry_lock` held, with explicit evidence that concurrent registry access is not blocked by the awaited I/O window. |
| `surface taxonomy PASS implied downstream public surface classification was complete` | Surface review passed on named `tela.*` MCP/CLI/HTTP surfaces, but `docs/DESIGN.md` still documented `re_enumerate(server_name)` as downstream public API without any re-close decision on whether it is external contract, compatibility shim, or internal adapter. The earlier pass therefore no longer counts as full closure. | The review scope was too narrow: it verified named agent-facing surfaces only and never required all documented public helpers to be classified. That let a blocker-class surface ambiguity survive behind a passing taxonomy review. | Any future surface audit that only checks branded/product entry points can miss symbol-level public-surface drift documented elsewhere. | `debt_closure.contract_test` | Re-close requires an explicit public-surface classification for `re_enumerate()` plus matching contract proof: either docs/tests confirm it as supported external behavior, or docs/internal annotations remove it from public surface claims. |
| `manifest/dependency review implicitly treated FastMCP authority as settled` | Earlier closure allowed three incompatible authorities to coexist: package authority (`fastmcp>=2.0.0` in `pyproject.toml`), repro dependency authority (`from fastmcp import FastMCP`), and runtime import authority (`from mcp.server.fastmcp import FastMCP`). With upgraded control-plane rules, this can no longer be treated as closed because the exact authority boundary was never recorded. | Reviews were fragmented: packaging, manifest/header wording, and runtime imports each passed locally, but no gate required a single canonical authority tuple across code, tests, and user-facing manifest language. | The same weakness could recur for any dependency whose package name, canonical import path, and runtime-facing wording differ across layers. | `debt_closure.impl` | Re-close requires one authoritative dependency record tying package declaration, canonical import path, runtime import policy, and manifest wording together, with code/tests/docs aligned or explicitly translated at the boundary. |
