## Surface And Authority Decision Record

### refs Read Confirmation
- `docs/adr/ADR-006-downstream-recovery.md` — NOT READ: file does not exist in this worktree; nearest matching ADR read was `docs/ADR-006-steady-state-downstream-recovery.md`, which states recovery must reuse the existing single-server reconnect/convergence path and avoid inventing a second authority.
- `docs/DESIGN.md` — key passage: `downstream.py` currently lists `re_enumerate(server_name: str) -> Result[list[ResolvedTool], str]` under `Public API`, while gateway dependencies already name `mcp.server.fastmcp` as the FastMCP import path used by runtime shell modules.
- `docs/INTERFACES.md` — key passage: section `Downstream fastmcp Client Contract` binds downstream session behavior to `mcp.client`/fastmcp clients, and section `MCP Surface` makes operator/manifest surfaces authoritative through documented user-facing contracts rather than package-import wording.
- `pyproject.toml` — key passage: runtime dependency authority is `fastmcp>=2.0.0`; there is no separately declared `mcp` package dependency.
- `src/tela/shell/downstream.py` — key passage: `re_enumerate()` is a live exported function and `reload.py` imports it as `_manual_reenumerate_adapter = re_enumerate`, so it is neither dead nor purely hypothetical.

| decision_ref | current_ambiguity | chosen_authority | rejected_alternatives | change_required | exact_files_if_change | no_change_evidence | verification_evidence_required | closure_path |
|---|---|---|---|---|---|---|---|---|
| SURFACE-REENUMERATE | `docs/DESIGN.md` declares `re_enumerate()` as public API, but the function docstring does not explicitly classify it and the probe treats that silence as blocker debt. | Classify `re_enumerate()` as a **supported public surface** for the shell module boundary. Basis: existing `docs/DESIGN.md` public-API declaration, live export in `src/tela/shell/downstream.py`, and production consumption from `src/tela/shell/reload.py` as the manual re-enumeration adapter. | 1. **Framework-only escape hatch** — rejected because the symbol is already documented as public API and is consumed outside a framework callback boundary. 2. **Dead export to remove** — rejected because the symbol is importable and wired into reload orchestration. | yes | `src/tela/shell/downstream.py` (docstring must say supported public surface), `docs/DESIGN.md` (make classification explicit, not just implied by list membership), `evidence/runtime_uncertainty_register.md` or successor decision ledger (record resolved classification for gate review). | Runtime behavior already matches the chosen classification: `re_enumerate()` exists, is importable, and `src/tela/shell/reload.py` binds it as `_manual_reenumerate_adapter`. No functional code-path change is required. | 1. `read`/`grep` evidence showing explicit supported-public-surface language in `re_enumerate()` docstring; 2. design doc line showing matching public-surface classification; 3. probe evidence that importability still holds and classification audit no longer fails. | Carry this decision into `debt_closure.impl.apply_surface_and_manifest_resolution`; after doc-only alignment, rerun `tests/repro/test_adr006_runtime_hardening_probes.py` and feed resulting pass evidence into `debt_closure.verify.wiring_and_manifest_audit` and `debt_closure.final_review.wiring_rereview`. |
| AUTH-MCP-FASTMCP | Dependency declaration says `fastmcp>=2.0.0`, runtime shell imports `FastMCP` from `mcp.server.fastmcp`, and repro test text claims `from fastmcp import FastMCP` is canonical; no single translation rule is documented. | Adopt an explicit **translation-boundary authority tuple**: package authority = `fastmcp` distribution in `pyproject.toml`; runtime import authority inside tela shell = `mcp.server.fastmcp.FastMCP`; manifest/header documentation authority = `src/tela/shell/surface_instructions.py`, which remains implementation-agnostic and must not claim a different import path. Authoritative design write-up should live in `docs/DESIGN.md` (with matching note in `docs/INTERFACES.md`). | 1. **Make `from fastmcp import FastMCP` the sole canonical authority everywhere** — rejected because current runtime shell, fixtures, and integration tests consistently use `mcp.server.fastmcp` and no repo-local evidence proves a safe bulk switch is required. 2. **Treat current split as acceptable without documentation** — rejected because the proof contract requires one reconciled tuple or an explicit translation boundary. 3. **Change package authority from `fastmcp` to `mcp`** — rejected because `pyproject.toml` declares only `fastmcp`. | yes | `docs/DESIGN.md` (record authority tuple), `docs/INTERFACES.md` (record translation boundary or import-policy note), `tests/repro/test_medium.py` (stop asserting top-level import path is the sole canonical runtime authority), `tests/repro/test_adr006_runtime_hardening_probes.py` (probe must accept the documented translation boundary), and only if docs currently leak import wording into user-facing manifest text: `src/tela/shell/surface_instructions.py`. | Runtime code is already internally consistent on the chosen runtime path: `src/tela/shell/gateway.py` and `src/tela/shell/gateway_runtime.py` both import `FastMCP` from `mcp.server.fastmcp`, and current manifest/header text does not mention any conflicting import path. | 1. cited dependency proof from `pyproject.toml`; 2. cited runtime import proof from shell modules; 3. docs evidence naming the tuple and saying manifest text is import-path-neutral; 4. updated probe/test evidence showing the translation boundary is now treated as authoritative rather than a split. | Carry this decision into `debt_closure.impl.apply_surface_and_manifest_resolution`; then rerun the repro authority probe plus any manifest-surface tests before `debt_closure.verify.wiring_and_manifest_audit` and `debt_closure.final_review.final_gate`. |

## Conflict Notes
- docs/code conflict: dispatcher required `docs/adr/ADR-006-downstream-recovery.md`, but this worktree contains `docs/ADR-006-steady-state-downstream-recovery.md`; decision basis used the extant ADR file and records the path mismatch as a documentation-location conflict.
- metadata/import conflict: dependency metadata names `fastmcp`, runtime shell imports `mcp.server.fastmcp.FastMCP`, and repro packaging proof currently asserts `from fastmcp import FastMCP` as canonical; this is resolved only by documenting the explicit package-to-runtime translation boundary above.
