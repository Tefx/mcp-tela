# Runtime Uncertainty Register

### Input Artifact Reviewed
- behavioral proof register from `debt_closure.runtime_evidence.collect_behavioral_proof`: `evidence/behavioral_proof_register.md`

| item_ref | provenance | disposition | evidence_sufficient | why | owner_step | closure_path | review_miss_cause | overturned_decision | re_close_proof_required |
|---|---|---|---|---|---|---|---|---|---|
| R13 | pre-existing | blocking-now | no | `evidence/behavioral_proof_register.md` still carries R13 with a skipped runtime tracing probe and explicitly says static code reading alone cannot close the obligation. That leaves the required runtime witness for "no _registry_lock held during awaited network I/O" unproven. | debt_closure.impl.close_runtime_gap_if_exposed | Runtime tracing/contention proof -> update `evidence/behavioral_proof_register.md` R13 row to `PROVEN` -> re-run this register with R13 re-dispositioned -> debt_closure.verify.runtime_evidence_r13_r42 -> debt_closure.final_review.behavior_rereview -> debt_closure.final_review.final_gate | Prior review accepted code-structure analysis as closure shape for a behavioral lock-safety claim even though the upgraded review basis requires runtime witness evidence. | `ADR-006 deep conformance closure treated as done despite SUCCESS + NEEDS_TEST for R13 no-lock-during-await` | Runtime tracing or contention evidence showing awaited network operations proceed while `_registry_lock` is released and concurrent registry access is not blocked during the await window. |
| R42-CONFIG-REMOVE-INFLIGHT | pre-existing | blocking-now | no | The behavioral proof register records only a unit-level prune pass plus an `XFAIL` for config-reload-remove during in-flight recovery. The review basis says the in-flight removal case is blocker-class until runtime witness evidence closes it. | debt_closure.impl.close_runtime_gap_if_exposed | In-flight config-remove runtime witness -> update `evidence/behavioral_proof_register.md` with explicit `config_missing=true` and no stale lock evidence -> re-run this register -> debt_closure.verify.runtime_evidence_r13_r42 -> debt_closure.final_review.behavior_rereview -> debt_closure.final_review.final_gate | Earlier closure let `SUCCESS` coexist with an embedded `NEEDS_TEST` runtime invariant instead of forcing either live proof or explicit carried debt ownership. | `ADR-006 deep conformance closure treated as done despite SUCCESS + NEEDS_TEST for R42 lock-cleanup` | Live runtime evidence showing config reload removes the server during recovery, surfaces `config_missing=true`, and leaves no stale per-server recovery lock after cleanup. |
| R42-DISCONNECT-UNDER-RECOVERY | pre-existing | blocking-now | no | The closure contract requires both R42 paths: config-reload-remove during in-flight recovery and disconnect under recovery pressure. The current behavioral proof register does not provide a separate disconnect-path witness row, so this path remains undispositioned and unproven. | debt_closure.impl.close_runtime_gap_if_exposed | Disconnect-path runtime witness -> expand `evidence/behavioral_proof_register.md` so the disconnect case is explicit rather than folded into the summary R42 row -> re-run this register -> debt_closure.verify.runtime_evidence_r13_r42 -> debt_closure.final_review.behavior_rereview -> debt_closure.final_review.final_gate | Earlier review allowed the two-path R42 obligation to stay collapsed into one summary row, which hid that the disconnect path never received its own proof disposition. | `R42 closure assumption treated partial config-remove evidence as enough for the full two-path obligation` | Runtime evidence for the disconnect-under-recovery path showing cleanup completes with no stale lock state after disconnect pressure; re-close must cite this path separately from config-reload-remove. |
| UNC-LIVENESS-HEALTHY-NEIGHBOR | pre-existing | non-blocking | yes | The current evidence includes a passing per-server lock partitioning probe and no exposed defect. The skipped live two-server witness remains a confidence-improver, but the present record is sufficient to treat this adjacent edge case as non-blocking while R13/R42 themselves remain blocked on direct runtime proof. | N/A — already sufficiently dispositioned for current gate shape | Monitor only; if later review demands live two-server witness, reopen under `debt_closure.impl.close_runtime_gap_if_exposed` before `debt_closure.verify.runtime_evidence_r13_r42`. |  |  | Existing per-server lock proof is sufficient for this adjacent edge case; reopen only if a later remediation changes lock-scoping behavior. |
| UNC-CONFIG-MISSING-FAIL-CLOSED | pre-existing | non-blocking | yes | The behavioral proof register cites a passing probe that `_get_runtime_server_config()` fails closed with `config_missing=True`, and no contradictory permissive path is exposed in the current record. The remaining blocker is integrated R42 cleanup proof, not this supporting fail-closed semantic. | N/A — already sufficiently dispositioned for current gate shape | No remediation required unless later runtime evidence contradicts the helper-level fail-closed result. |  |  | Re-close only if later remediation alters missing-config handling or contradicts `config_missing=true`. |
| SURFACE-REENUMERATE | out-of-step-origin | **CLOSED-2026-04-07** | **yes** | `re_enumerate()` is explicitly classified as `RESOLVED_EXTERNAL_CONTRACT` per `docs/DESIGN.md:620` which documents it as "Supported public surface". Classification propagated to runtime uncertainty register as part of surface-reenumerate reclose synchronization. No additional docs/tests/annotation changes required—existing documentation satisfies surface contract requirements. | debt_closure.impl.decide_surface_and_manifest_authority | **CLOSED**: Surface classification verified against DESIGN.md public API documentation. | The earlier surface review scoped itself to named `tela.*` MCP/CLI/HTTP surfaces and never forced a classification for helpers documented as public API in `docs/DESIGN.md`. | `surface taxonomy PASS implied downstream public surface classification was complete` | **CLOSED**: Runtime uncertainty register synchronized—`re_enumerate()` classification resolved via existing DESIGN.md documentation. |
| AUTH-MCP-FASTMCP | out-of-step-origin | **CLOSED-2026-04-07** | **yes** | Authoritative tuple documented in `docs/DESIGN.md:561-578` under "FastMCP Translation Boundary" section: package=`fastmcp>=2.0.0` (pyproject.toml), runtime import=`from mcp.server.fastmcp import FastMCP` (gateway.py, gateway_runtime.py), manifest/header authority=implementation-agnostic (surface_instructions.py). Translation boundary explicitly established—tela shell modules use `mcp.server.fastmcp` as internal path; tests may use either path per context. | debt_closure.impl.decide_surface_and_manifest_authority | **CLOSED**: Authority tuple verified across pyproject.toml, DESIGN.md translation boundary section, and runtime imports. | Earlier reviews let packaging proof, runtime imports, and manifest wording pass independently without requiring one reconciled authority record across artifacts. | `manifest/dependency review implicitly treated FastMCP authority as settled` | **CLOSED**: Runtime uncertainty register synchronized—FastMCP authority tuple resolved via existing DESIGN.md translation boundary documentation. |

## Provenance vs Disposition Check
- any item marked pre-existing but blocking-now? yes — `R13`, `R42-CONFIG-REMOVE-INFLIGHT`, and `R42-DISCONNECT-UNDER-RECOVERY` are blocker debt now because current evidence is insufficient and these items intersect `debt_closure.verify.runtime_evidence_r13_r42` and `debt_closure.final_review.final_gate`; `pre-existing` is used only as provenance, not as a softening disposition.

## Normalized Blocker Basis Rule
- The authoritative blocker families are exactly: `R13`, `R42-CONFIG-REMOVE-INFLIGHT`, `R42-DISCONNECT-UNDER-RECOVERY`, `SURFACE-REENUMERATE`, and `AUTH-MCP-FASTMCP`.
- `gate_open_allowed=false` until downstream reclose verification closes those blocker rows in the structured evidence set; documenting a blocker without closing it is not enough.
- No blocker-class item may be downgraded to non-blocking unless explicit non-intersection evidence names the remaining gates it does not intersect. No such downgrade evidence exists in the current record for any of the five blocker families.
- The earlier closure was invalid because it treated documented-but-open blocker rows as compatible with success instead of requiring those rows to be closed before gate-open.

## Carry-Forward Blockers
- `R13` -> `debt_closure.impl.close_runtime_gap_if_exposed`
- `R42-CONFIG-REMOVE-INFLIGHT` -> `debt_closure.impl.close_runtime_gap_if_exposed`
- `R42-DISCONNECT-UNDER-RECOVERY` -> `debt_closure.impl.close_runtime_gap_if_exposed`

## Synchronization Notes (2026-04-07)
- `SURFACE-REENUMERATE`: **CLOSED** - Verified as `RESOLVED_EXTERNAL_CONTRACT` via `docs/DESIGN.md:620` public API documentation
- `AUTH-MCP-FASTMCP`: **CLOSED** - Verified via `docs/DESIGN.md:561-578` FastMCP Translation Boundary authority tuple documentation
- Remaining three blocker families (`R13`, `R42-CONFIG-REMOVE-INFLIGHT`, `R42-DISCONNECT-UNDER-RECOVERY`) remain open pending runtime witness evidence per ADR-006 debt closure requirements
